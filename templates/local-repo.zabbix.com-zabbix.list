# Only for adding zabbix extensions / packages. Upgrades will be done by
# ansible mit.zabbix-server. We use http instead of https in order to be ignored by ansible.
# deb [signed-by=/usr/share/keyrings/zabbix-official-repo.gpg] http://repo.zabbix.com/zabbix/{{ zabbix_server_major_version }}/debian {{ ansible_distribution_release }} main

